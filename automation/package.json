{
  "name": "automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:frontend": "playwright test --project=chrome",
    "test:api": "playwright test --project=rest",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:smoke": "playwright test --grep @smoke",
    "test:regressao": "playwright test --grep @regressao",
    "test:negativo": "playwright test --grep @negativo",
    "test:contrato": "playwright test --grep @contrato",
    "report": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0"
  },
  "dependencies": {
    "@faker-js/faker": "^10.4.0",
    "jwt-decode": "^4.0.0",
    "pg": "^8.20.0"
  }
}
